use of ```setTimeout``` as an offset for the flow of the game (especially when adding an additiona
Simon move after player moves)

inset in diplay, x=220 y=290 is upper left corner

manipulating text in svg
http://stackoverflow.com/questions/14758125/setting-text-svg-element-dynamically-via-javascript



1. issues with start of simon's turn and end of players input:
 
2. issues with the fail sound...


both solved through use of ```setTimeout(function(){new function call}, time)``` 230


removing child element with ```winnerText.parentNode.removeChild(winnerText);```


102 assigning a local variable to the name of the element in color press was necessary. Why?


playSound and the call to ```mute``` not working but ```changeColorOff``` does

